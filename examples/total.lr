-- What a loop looks like when something has to survive it.
var.local.mut.ui32 ['total'] = [|0|];

loop.temp.range.ui32 ['i'] = [|1|, |10|] {
    handback 'i' as 'total';
}

print["total is " 'total' \n];
