/target
**/*.rs.bk
.DS_Store

# Compiled chunks. Built from the sources beside them.
*.lrc

# Where the author writes Luarust. Not part of the language, and not `ts` -- that is the
# home directory nearly every project here sits under, and one of them should not be it.
/author-test/
